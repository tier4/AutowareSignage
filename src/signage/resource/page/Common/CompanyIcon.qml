import QtQuick 2.9

Image {
    id: tier4Icon
    width: 272*viewController.size_ratio
    height: 68*viewController.size_ratio
    source: "../icon/tier4_icon.png"
    anchors.top: parent.top
    anchors.topMargin: 10*viewController.size_ratio
    anchors.right: parent.right
    anchors.rightMargin: 10*viewController.size_ratio
}
